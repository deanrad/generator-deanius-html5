language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-deanius-html5' ]; then cd .. && eval "mv $currentfolder generator-deanius-html5" && cd generator-deanius-html5; fi

